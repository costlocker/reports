#!/bin/sh

phpqa --report --verbose \
    --tools phpcs:0,phpmd:5,phpcpd:0,phpmetrics,phploc,pdepend,phpunit:0,security-checker:0 \
    --buildDir var/QA \
    --analyzedDirs ./ \
    --ignoredDirs vendor
